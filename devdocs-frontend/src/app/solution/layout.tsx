/**
 * Solutions section layout.
 * Shared layout for all solution-related pages with navigation breadcrumbs.
 */

export default function SolutionLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
